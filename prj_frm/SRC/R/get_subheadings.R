##############################################
## parse subheadings to a separate txt file ##
##############################################

getMandatoryKeys <- function(frm.dat) {
    tmp <- NULL
    
    lapply(frm.dat, function(x)) {
        n <- names(x)
        if(!is.null(tmp)) {
            tmp <- 
        }
    }
}
